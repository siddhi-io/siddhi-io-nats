
# Simple TLS config file

port: 4222
net: localhost

tls {
  cert_file:  "tls/certs/server-cert.pem"
  key_file:   "tls/certs/server-key.pem"
  timeout:    2

  # Optional certificate authority for clients
  ca_file:   "tls/certs/ca.pem"
}
